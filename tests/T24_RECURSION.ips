#include "inpa.h"

fact(in: int n, out: int result)
{
  int tmp;
  if (n <= 1) {
    result = 1;
  } else {
    fact(n - 1, tmp);
    result = n * tmp;
  }
}

inpainit()
{
  int res;
  fact(4, res);
}

inpaexit()
{
}
