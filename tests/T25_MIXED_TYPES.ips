#include "inpa.h"

T25_mixed_types()
{
  int i;
  real r;
  string s;
  bool b;

  i = 5;
  r = 2.5;
  b = (i > 0);
  realtostring(r, "2.1", s);
  if (b) {
    i = i + 1;
  }
  inttostring(i, s);
}

inpainit()
{
}

inpaexit()
{
}
