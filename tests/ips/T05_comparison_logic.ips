#include "inpa.h"

inpainit()
{
    int a;
    int b;
    bool r;

    a = 1;
    b = 2;

    r = (a == b);
    r = (a != b);
    r = (a < b);
    r = (a > b);
    r = (a <= b);
    r = (a >= b);
    r = (a < b) && (b > 0);
    r = (a < b) || (b > 0);
    r = !(a == b);
}

inpaexit()
{
}
