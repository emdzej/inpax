#include "inpa.h"

int g_sum;

T13_nested_loops()
{
  int i;
  int j;
  g_sum = 0;
  i = 0;
  while (i < 3) {
    j = 0;
    while (j < 2) {
      g_sum = g_sum + i + j;
      j = j + 1;
    }
    i = i + 1;
  }
}

inpainit()
{
}

inpaexit()
{
}
